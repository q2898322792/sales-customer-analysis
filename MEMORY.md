# MEMORY.md — 销售与客户专题分析（技术说明）

> 记录本项目的技术决策与踩过的坑，供后续维护参考。
> 本项目与 [制造业经营分析数据仓库](https://github.com/q2898322792/manufacturing-dw) 解耦：
> 默认读项目自带的 SQLite 数据集，也可切换为只读数仓 ADS 层。

---

## 一、项目概况

| 项 | 内容 |
|---|---|
| 数据集 | 项目自带 `data/sales.db`（SQLite），由 `src/make_data.py` 生成 |
| 可选数据源 | `--source mysql` 只读数仓 ADS 层（不写回任何表） |
| 技术栈 | Python（pandas / numpy / matplotlib / pymysql）· SQL |
| 依赖说明 | 未使用 scikit-learn —— KMeans 为 numpy 手写实现，不引入该依赖 |
| 一键运行 | `cd src && python run_all.py` |

## 二、目录结构

```
src/
├── config.py       # 路径与连接配置（唯一来源）+ matplotlib 中文字体
├── make_data.py    # 生成自带数据集（混合分布客户 + 泊松购买过程 + 植入异常）
├── extract.py      # 双数据源统一取数（sqlite / mysql），下游无感
├── rfm.py          # RFM 五分位打分 + 八分层
├── kmeans.py       # KMeans（numpy 手写，k-means++ + 固定种子 42）+ 手肘法 + 簇命名
├── anomaly.py      # 稳健 z-score（中位数 + MAD + 业务幅度门槛）+ 帕累托
├── visualize.py    # 5 张图
└── run_all.py      # 一键编排，末尾生成报告
销售与客户专题分析报告.md          ← 完整报告（8 节），数字的唯一来源
output/
├── customer_segment_result.csv  ← 每家客户的 R/F/M、评分、分层、聚类归属
├── monthly_sales.csv            ← 月度营收 / 订单数 / 单均金额 / 异常标记
└── figures/ ×5                  ← 01 散点 / 02 分层条形 / 03 帕累托 / 04 月度趋势异常 / 05 聚类
```

## 三、数据构造：三个必须避开的坑

数据刻意按真实业务特征构造（客户规模混合分布 + 泊松购买过程 + 植入已知异常），
过程中踩过三次坑，都是"集中度被二次放大"：

| # | 错误做法 | 后果 | 正确做法 |
|---|---|---|---|
| 1 | 订单数 ∝ scale，**且单均金额也 ∝ scale** | 营收 ∝ scale²，Top20% 冲到 **97%** | 金额与规模**弱相关**（仅 ±10% 随机） |
| 2 | `订单数 ∝ scale × activity` | Top20% 仍达 **80%** | `订单数 ∝ scale` 单独决定；activity / gap 只用于制造 R 维度差异 |
| 3 | 客单价与规模**完全独立** | `r(F,M)` 掉到 0.44，M 与 F 失去应有的正相关 | 保持**弱正相关**（弹性 0.15 + 客户级对数正态噪声） |

**参数定标**（`PRICE_CUST_SIGMA` → `r(F,M)` 实测扫描）：

```
0.35 -> 0.86    0.45 -> 0.80    0.55 -> 0.70    0.95 -> 0.44（过度解耦）
取 0.45，使 r(F,M) ≈ 0.80
```

## 四、异常检测：为什么需要两道门槛

`detect_anomaly` 同时要求两个条件才判为异常：

1. `|z| ≥ 2.5`（稳健 z-score，中位数 + MAD —— 比均值 + 标准差更抗离群值污染）
2. `|偏离中位数| ≥ 15%`（业务幅度门槛）

**只靠 z 值会误报**：当数据本身极稳定时 MAD 趋近 0、z 值会爆炸，
实测把偏离 0.2% 的波动判成了异常。

## 五、v1 → v2：四个数据源缺陷的修复

v1 的图表效果不佳，逐图排查后确认**主因是数据源缺陷而非绘图代码**：

| 缺陷 | v1 实测 | v2 修法 | v2 结果 |
|---|---|---|---|
| **A. M 与 F 完全共线** | `r(F,M)=0.999984`（散点退化成直线，RFM 的 M 沦为 F 的重复） | 客单价改客户级对数正态，与规模仅弱相关（弹性 0.15） | **r = 0.7965** |
| **B. 金额分布过极端** | 偏度 8.31、max/中位 55 倍 | 混合分布（85% 对数正态主体 + 15% 头部） | 偏度 **1.86**、max/中位 **12.4** |
| **C. R 不连续** | R=0 占 85%、八分层只有 6/8 类非空 | 泊松过程（指数分布购买间隔） | R 取值 **72** 个、八分层 **8/8 类非空** |
| **D. 只植入 1 处异常** | 仅一个异常月 | 三种形态：上偏 / 下偏 / 强上偏 | 三种全部检出，与植入值吻合 |

**视觉侧同步修复**（`visualize.py`）：

| 图 | 修复 |
|---|---|
| 01 | M 轴「亿元」→「万元」（消除 0.02/0.08 这类难读刻度）；图例按客户数降序 |
| 02 | 纵向柱状 → **横向条形**（中文标签水平可读）；断崖折线 → 条上直标读数 |
| 03 | 去掉被压平的柱状（帕累托核心是累计曲线），改标注 Top10 / Top20% / 第 N 家达 80% |
| 04 | 右轴改万元去千分位；顶部留白 + 标注**按点位自适应避让** |
| 05 | 图例按客均营收降序；加**簇质心 ✕** 标记 |

**附带收益**：项目可独立复现 —— `clone` → `make_data.py` → `run_all.py` 即可出报告，
不依赖本地 MySQL 与数仓。

## 六、文档同步约定

| 文档 | 放什么 | 不放什么 |
|---|---|---|
| `README.md` | **设计常量** + **定性结论** | 会随数据重新生成而变化的数值 |
| `销售与客户专题分析报告.md` | **所有数值 —— 数字的唯一来源** | — |

**为什么这样定**：表格数值能自动生成，但**叙述文字里的数字**不会 ——
改数据后极易出现"报告说 A、README 说 B"的矛盾。所以让数字只在报告里维护一处。

**报告内部**：关键数字一律从实测值动态生成（`vmed` / `vratio` / `cluster_means` /
`top2_pct` / `rfm_top_pct` / `m_skew` / `m_ratio` / `n_inj` / `inj_desc`），模板里只保留措辞。

**仍无法自动同步的（改数据后需人工核对）**：
1. 叙述性措辞（如"较为集中"）
2. 交叉引用（"见第七节" —— 章节号变了不会跟）
3. 表格列名与单位
4. 图表标题 / 图例 / 轴标签（在 `visualize.py` 里硬编码）

## 七、Lint 与运行环境

- 全篇中文标点需统一；技术名大小写与空格要一致（`MySQL 8.0` / `FineBI 6.0`）
- 图表文件名统一 ASCII（配合 jsDelivr CDN 在 README 中引用）
- `.gitignore` 忽略 `__pycache__/`、`*.pyc`、`.venv/`、`data/*.db`
