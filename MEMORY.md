# MEMORY.md — 销售与客户专题分析（接手说明）

> **新会话接手时，先读本文件。** 本项目与 `D:\data_warehouse_project`（数仓项目）**完全独立**，
> 只**只读**数仓 ADS 层，不写回任何表。

---

## 一、项目定位

- **目录**：`D:\sales_customer_analysis\`（独立 git 仓库，首次 commit `2c02f7f`）
- **用途**：用户的**第二个简历项目**，投「数据分析专员」岗（校园招聘，22 号开始）
- **背景**：用户**无实习 / 无课程经历**，只有一个数仓项目 → 需要做第二个项目撑场面
- **姊妹项目**：[制造业经营分析数据仓库](https://github.com/q2898322792/manufacturing-dw)（提供数据）
- **技术栈**：Python（pandas / numpy / matplotlib / pymysql）· SQL
  - ⚠️ **本机未安装 scikit-learn** → KMeans 用 numpy 手写，不要引入该依赖

## 二、目录结构

```
src/
├── config.py       # 数据库连接（读环境变量 DB_HOST/PORT/USER/PASSWORD）+ matplotlib 中文字体
├── extract.py      # 从 ads_db.ads_sale_analysis 只读取数（客户RFM / 月度汇总 / 预警分布）
├── rfm.py          # RFM 五分位打分 + 八分层（SEGMENT_RULES 定义 8 类）
├── kmeans.py       # KMeans（numpy 手写，k-means++ 初始化 + 固定种子 42）+ 手肘法
├── anomaly.py      # 稳健 z-score 异常检测（中位数+MAD，阈值 2.5）+ 帕累托
├── visualize.py    # 5 张图
└── run_all.py      # 一键编排，末尾生成报告
output/
├── 销售与客户专题分析报告.md     ← 8 节完整报告
├── customer_segment_result.csv  ← 300 家客户 R/F/M、评分、分层、聚类归属
├── monthly_sales.csv            ← 月度营收/订单数/单均金额/异常标记
└── figures/ ×5（01 RFM散点 / 02 八分层柱状 / 03 帕累托 / **04 月度趋势异常（核心图）** / 05 聚类散点）
README.md           # 项目说明（简历门面）
requirements.txt / .gitignore
```

**运行**：`cd src && python run_all.py`（Python 用 `D:\python11\python3.10.11\python.exe`）

## 三、已完成（B 版，commit `2c02f7f`）

1. **经营异常检测（主线）**：月订单量稳定（CV=0.125），但**单均金额波动 5 倍**；
   稳健 z-score 检出 4 个异常月：**2025-12 +115% / 2026-01 +122% / 2026-02 −58% / 2026-06 +94%**
2. **RFM 八分层**：五分位打分 + 8 类；帕累托显示 Top10 仅占营收 **3.5%**、Top20% 占 **20.6%**
3. **KMeans 聚类交叉验证**（k=4）：与规则分层结论一致
4. **预警分布**：回款滞后 30 条 + 营收缺口 25 条 = 占 **70%** → 风险在资金链
5. 报告第七节「**数据适用性说明**」如实标注数据局限（见下）

## 四、🔴 三个关键发现（务必记住）

### 1. 仿真数据的客户是同质的 —— RFM 在这批数据上不成立

```
recency   : 全部 = 0        ← 每家客户每天都下单，R 维度完全无信息
frequency : 变异系数 1.7%
monetary  : 变异系数 2.1%
KMeans 四簇均值：8.96亿 / 8.77亿 / 8.61亿 / 8.42亿 —— 只差 4%
```

真实业务客户应呈**幂律分布**（Top20% 贡献 60~80%），本数据 Top20% 只贡献 20.6%。
→ **简历上不能写"分成 8 个价值层级"**，除非先做 A 修正数据（会被问"各层差异多大"，答案是 4%）。

### 2. SQL bug：`%%` 转义（已修）

`pandas.read_sql` **不带参数时** `%%Y-%%m` 不会被转义 → `DATE_FORMAT` 返回空、12 个月被合成 1 组。
**正确写法是单 `%`**（`%Y-%m`）；`%%` 只在带参数时需要。

### 3. 真信号在时间维度，不在客户维度

异常检测（5 倍波动）是**真实且有说服力的**结论，不受客户同质性影响 → 报告把它放在第二节作主线。

## 五、✅ 方案 A′ 已完成：第二个项目自带造数（数仓项目零改动）

用户提出「为什么不把代码复制到第二个项目里改」→ 点出关键：**代码副本 ≠ 数据隔离**，
生成器写的是 MySQL 源库，复制代码照样会改数仓数据。

**最终做法 A′+**：第二个项目**自己造数**写进项目内 SQLite → 数仓项目**全程零改动** ✅

### 新增文件

- `src/make_data.py`：生成自带数据集 `data/sales.db`（SQLite）
  - 客户金额规模：幂律 `w ∝ i^(-0.8)` → Top20% 贡献 ~62%
  - 购买活跃度与规模正相关，底部 12% 客户**沉睡**（gap 60~180 天）→ R 有区分度
  - **刻意植入异常**：2026-06 单均金额 ×2.2（作为异常检测的 fixture，文档已标注）
  - 约 80 万行，生成约 1 分钟
- `extract.py` 改为**双数据源**：`--source sqlite`（默认，可独立复现）/ `mysql`（直连数仓 ADS 层）

### 🔴 造数踩过的两个坑（务必记住）

1. **集中度会二次放大**：订单数 ∝ scale，若单均金额也 ∝ scale → 营收 ∝ scale²，
   实测 Top20% 冲到 **97%**。修正：金额与规模**弱相关**（只加 ±10% 随机）。
2. **再乘 activity 又会放大**：`raw = scale × activity` → Top20% 仍 80%。
   修正：`raw = scale` 单独决定订单数；activity / gap 只用于制造 R 维度差异。
   → 最终 Top10 31.9% / Top20% 61.9%（目标 60%）✅

### 🔴 异常检测误报

新数据集金额极稳定时 **MAD → 0，z 值爆炸**，把偏离 0.2% 的波动判成异常。
修正：`detect_anomaly` 加 **`min_deviation=15%`** 业务幅度门槛，与 z 值**同时**满足才算异常。

### ⚠️ v1 的四个数据源缺陷 —— 已于 v2 全部修复（commit `3151560`）

逐图排查（见 `docs/图表诊断与改进.md`）发现：图表效果不佳的主因是数据源缺陷。v2 修正如下：

| 缺陷 | v1 实测 | v2 修正 | v2 结果 |
|---|---|---|---|
| **A. M 与 F 完全共线** | `r(F,M)=0.999984`（散点退化成直线、RFM 的 M 沦为 F 的重复） | 客单价改客户级对数正态，与规模仅弱相关（弹性 0.15） | **r=0.7965** |
| **B. 金额分布过极端** | 偏度 8.31、max/中位 55 倍 | 混合分布（85% 对数正态主体 + 15% 头部） | 偏度 **1.86**、max/中位 **12.4** |
| **C. R 不连续** | R=0 占 85%、八分层只有 6/8 类非空 | 泊松过程（指数分布间隔） | R 取值 **72** 个、R=0 占 26%、八分层 **8/8 全满** |
| **D. 只植入 1 处异常** | 仅 2026-06 | 三种形态 +60% / −55% / +120% | 检出 **+58.9% / −56.4% / +118.5%** |

**参数定标过程**（`PRICE_CUST_SIGMA` 与 `r(F,M)` 的关系，实测扫描）：

```
0.35 -> 0.86    0.45 -> 0.80    0.55 -> 0.70    0.95 -> 0.44（过度解耦）
取 0.45，使 r(F,M) ≈ 0.80 —— 既保留"大客户贡献更多"的正相关，又让 M 有独立信息
```

**规模（按用户要求收敛）**：79.9 万行 → **4.9 万行**；158.87 亿元 → **3.99 亿元**（单均 8,076 元）；SQLite 123 MB → 8 MB。

### 验证结果（v2）

```
异常检测：2026-01 +58.9% / 2026-02 -56.4% / 2026-06 +118.5%   ← 与植入值吻合 ✅
Top10 14.37% | Top20%(60家) 54.00% ✅     八分层 8/8 类非空 ✅
R 取值 72 个 | r(F,M)=0.7965 | M 偏度 1.86 ✅
```

### 视觉侧同步修复（`visualize.py`）

| 图 | 修复 |
|---|---|
| 01 | M 轴亿元 → **万元**（消除 0.02/0.08 这类难读小数刻度）；图例按客户数降序 |
| 02 | 纵向柱状 → **横向条形**（中文标签水平可读）；断崖折线 → 条上直标「N 家 · 营收 X%」；按营收占比排序 |
| 03 | **去掉被压平的柱状**（帕累托核心是累计曲线），改为标注 Top10 / Top20% / 第 N 家达 80% |
| 04 | 右轴改万元去千分位；顶部留 28% 空白 + 标注**按点位自适应避让**（修复"标注撞标题"）；最低点标注右下拉避免压线 |
| 05 | 图例按客均营收降序；加**簇质心 ✕** 标记，缓解重叠时看不出簇边界 |

### 附带收益

项目**可独立复现**：任何人 `clone` → `make_data.py` → `run_all.py` 即可出报告，
不再依赖本地 MySQL 和数仓 —— 这对 GitHub 项目是加分项。

## 六、简历文案（当前可用，已按 A′ 更新）

## 六、简历文案（当前可用）

```
销售与客户专题分析                          个人项目 · 2026.09
Python(pandas/numpy/matplotlib/SQLite) · SQL
自带 80 万行仿真销售数据（含幂律客户分布与已知异常），围绕客户价值分层与经营异常检测
两个主题，输出含图表、分层结果与运营建议的完整报告；项目 clone 即可复现，
并支持 --source mysql 直连数仓 ADS 层分析。

● 客户价值分层：RFM 五分位打分 + 八分层；帕累托分析显示 Top10 客户贡献营收 31.9%、
  Top20% 贡献 61.9%，识别重点维护与流失预警客群并给出差异化运营建议
● 异常检测：稳健 z-score（中位数+MAD，叠加 15% 业务幅度门槛）检出 2026-06 单均金额
  偏离 +120%，定位为「金额维度」异常而非交易活跃度波动
● 数据构造与验证：按真实业务特征构造幂律客户分布与沉睡客群（R 取值 0~177 天），
  并植入已知异常验证检测方法有效性；对比数仓均匀分布数据导致的分层失效，
  沉淀「先判断数据能否支撑结论」的分析习惯
● 聚类交叉验证：numpy 手写 KMeans（k-means++ + 固定种子），与规则分层结论一致
```

## 七、📌 文档同步约定（重要，务必遵守）

**README 与报告分工**（commit `c65f97a` 后确立）：

| 文档 | 放什么 | 不放什么 |
|---|---|---|
| **README.md** | **设计常量**（8 节 / 八分层 / 4 个簇 / 5 张图 / 两个数据源）+ **定性结论** | ❌ 任何**会随数据重新生成而变化**的数值 |
| **output/销售与客户专题分析报告.md** | **所有数值**（规模、占比、异常幅度、统计量）—— **数字的唯一来源** | — |
| `docs/*.md` | 诊断过程、方法说明、结论依据 | 数值以报告为准，引用时不写死 |

**为什么这样定**：本轮审计发现报告与 README 各有独立的一套数字，改数据后极易脱节。
表格数值能自动生成，但**叙述文字里的数字**不会 —— 所以让数字只在报告里维护一处。

**报告内部**：关键数字一律**从实测值动态生成**（`vmed` / `vratio` / `cluster_means` /
`top2_pct` / `rfm_top_pct` / `reclaim_*` / `m_skew` / `m_ratio` / `n_inj` / `inj_desc`），
模板里只保留**措辞**。改数据后若措辞与数字冲突，属人工检查项。

**仍无法自动同步的**（改数据后需人工核对）：
1. 叙述性措辞（如"较为集中""消费金额高"）
2. 交叉引用（"见第七节" —— 章节号变了不会跟）
3. 表格列名与单位
4. 图表标题 / 图例 / 轴标签（在 `visualize.py` 里硬编码）
5. `docs/*.md` 里的引用数字

## 八、数仓项目侧的相关约定（引用）

- FineBI 是**抽取模式**：改了数据必须手动「更新」，刷新浏览器无效
- `bl/` 是 PDF 原件（gitignore）；`docs/screenshots/` 是 PNG（进仓库，走 jsDelivr CDN）
- 数仓备份在 `D:\data_warehouse_project_backup\20260920-1321\`（改数据前可回滚）
